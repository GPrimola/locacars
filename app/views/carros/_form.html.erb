<%= form_with(model: [@unidade, @patio, carro], local: true) do |form| %>
  <% if carro.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(carro.errors.count, "error") %> prohibited this carro from being saved:</h2>

      <ul>
      <% carro.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :patio %>
    <%= form.label :patio, carro.patio %>
  </div>

  <div class="field">
    <%= form.label :fabricante %>
    <%= form.text_field :fabricante, id: :carro_fabricante %>
  </div>

  <div class="field">
    <%= form.label :nome %>
    <%= form.text_field :nome, id: :carro_nome %>
  </div>

  <div class="field">
    <%= form.label :modelo %>
    <%= form.text_field :modelo, id: :carro_modelo %>
  </div>

  <div class="field">
    <%= form.label :ano %>
    <%= form.number_field :ano, id: :carro_ano %>
  </div>

  <div class="field">
    <%= form.label :anoModelo %>
    <%= form.number_field :anoModelo, id: :carro_anoModelo %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
